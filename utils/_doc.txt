#uncommit
git reset HEAD^

#to run flask
export FLASK_APP=main.py
export FLASK_ENV=development
flask run

# to install mongodb
brew tap mongodb/brew
brew install mongodb-community@5.0

#to run mongodb as macos process
brew services start mongodb-community@5.0

#to stop mongodb (macos process)
brew services stop mongodb-community@5.0

#to verify that mongo is running (macos process)
brew services list

#mongo shell
show dbs
use dbs_name
show collections
db.collection_name.find()

